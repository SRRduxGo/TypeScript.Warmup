/**
 * hover on the variables to see their types
 */

const Dte = new Date();
const fn = function(kim, kar) {};
const fn_ = function(kim: number, kar: string): number {
  return 10;
};
const objs = {
  age: 10,
  name: "test"
};
class Color {}
const green = new Color();
